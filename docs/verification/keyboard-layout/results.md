# AC-30 / AC-04 검증 — 2026-09-17

기준 plan: f8e1d5e5fcd76af905809d8109c96fc8cfba39f9.

- PASS `npm test`: 3개 파일, 32개 테스트.
  - 각 키 묶음의 배타 코드, 8방향·90U/s·반대키 상쇄.
  - 기존/잘못된 설정 WASD 기본 복구, arrows 저장 복원.
  - 기존 차트·물리·판정·점수·기록 회귀 유지.
- PASS `npm run build`: TypeScript 및 Vite 프로덕션 빌드.
- PASS `npm run test:e2e`: Chrome 시나리오 7개 (23.8초).
  - 새 AC-30 시나리오: 기본값, 선택·리로드 복원, 비선택 키 무시,
    방향키 스크롤 차단, 실제 캐릭터 이동, 일시정지 중 선택 변경,
    재개 후 잔류 이동 없음, 한글 key 값과 물리 KeyD 처리,
    다른 대소문자 keyup 해제, 입력 필드 중 이동 없음, blur 초기화.
  - 기존 게임 시작·마우스·일시정지·결과 저장, 좌우 교환, 로딩 실패,
    모바일 동시 터치, 4색·타이머, 프로덕션 오프라인 재실행 회귀.
- PASS contract guard: worktree/index/HEAD 점검 후 전달.

화면 근거: setup.png (방향키 선택), paused.png (일시정지 중 WASD 변경).
실제 화면을 확인했으며, 기존 first-playable 화면 근거는 이번 테스트가 생성한
이미지로 덮어쓰지 않고 해당 커밋의 기존 파일을 유지한다.

브라우저 테스트의 일부 장면은 충돌을 제거한 fixture로 입력 자체를 검증한다.
실제 모바일 기기 성능 검증을 의미하지 않는다. Vite의 Phaser 큰 번들 안내는 기존과 동일하다.

## 사용성 수정 — 2026-09-17

사용자 제보: WASD가 작동하지 않고 설정에서 WASD 선택이 보이지 않음.
확인: 로컬 4173 포트에 실행 서버가 없었음. 캐시된 이전 화면이 남을 가능성이
있으나 사용자가 보고 있던 정확한 번들/저장값은 직접 확인하지 못했으므로 단정하지 않음.
기존 native select는 선택된 옵션 하나만 표시함.

수정: 시작·일시정지에 WASD/방향키 두 버튼을 항상 표시하고 aria-pressed로 선택 상태를
드러냄. 자동 저장 안내, 작은 화면의 폼 겹침/스크롤 수정. 설정된 키만 사용하는 계약 유지.
실제 WASD 키와 한글 key+물리 code 입력, 설정 전환, 저장 복원을 검증했다.
프로덕션 검증에도 두 버튼 표시와 WASD 선택 후 오프라인 리로드 저장 확인을 추가했다.

검증 중 실패: NodeList 순회 타입 오류는 Array.from으로 수정. 추가 컨트롤 때문에
좁은 화면에서 시작 버튼을 하단 문구가 가리는 문제는 form flex-shrink 해제와 스크롤로 수정.
동시에 실행된 테스트 프로세스가 서버/trace 파일을 정리한 실행은 폐기하고 단독 재실행했다.

최종 결과: 단위 테스트 32개 PASS, 최신 프로덕션 빌드 PASS,
브라우저 7개 PASS (20.6초). setup.png/paused.png는 두 버튼 버전의 최신 화면이다.
